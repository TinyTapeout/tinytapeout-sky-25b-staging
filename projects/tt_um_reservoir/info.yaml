# Tiny Tapeout project information
project:
  title: "EZ Reservoir" # Project title
  author: "kinako71-2" # Your name
  discord: "" # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description: "Lightweight linear reservoir ASIC implemented in Verilog." # One line description of what your project does
  language: "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz: 0 # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1" # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module: "tt_um_reservoir"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"
    - "reservoir.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "Input to reservoir [0]"
  ui[1]: "Input to reservoir [1]"
  ui[2]: "Input to reservoir [2]"
  ui[3]: "Input to reservoir [3]"
  ui[4]: "Input to reservoir [4]"
  ui[5]: "Input to reservoir [5]"
  ui[6]: "Input to reservoir [6]"
  ui[7]: "Input to reservoir [7]"

  # Outputs
  uo[0]: "Output from odd neuron [0]"
  uo[1]: "Output from odd neuron [1]"
  uo[2]: "Output from odd neuron [2]"
  uo[3]: "Output from odd neuron [3]"
  uo[4]: "Output from odd neuron [4]"
  uo[5]: "Output from odd neuron [5]"
  uo[6]: "Output from odd neuron [6]"
  uo[7]: "Output from odd neuron [7]"

  # Bidirectional pins
  uio[0]: "Output from even neuron [0]"
  uio[1]: "Output from even neuron [1]"
  uio[2]: "Output from even neuron [2]"
  uio[3]: "Output from even neuron [3]"
  uio[4]: "Output from even neuron [4]"
  uio[5]: "Output from even neuron [5]"
  uio[6]: "Output from even neuron [6]"
  uio[7]: "Output from even neuron [7]"

# Do not change!
yaml_version: 6
